// Different terms for inputs
function compressBoxesTwice(boxes1, boxes2) {
  boxes1.forEach((box) => {
    console.log(box);
  });

  boxes2.forEach((box) => {
    console.log(box);
  });
}

// O(n + m)
